# test-pod-laci.yaml
apiVersion: v1
kind: Pod
metadata:
  name: laci-test-pod
  namespace: student-laci
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        cpu: 100m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi